# Copy this file to .env and fill in real values.  .env is git-ignored.

# Where the DuckDB database file lives (created by the pipeline)
SETTLEMENT_DB=data/warehouse/settlement.duckdb

# Key the dashboard / API callers must send in the X-API-Key header.
# Generate one with:  python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=change-me-to-a-long-random-string

# Shown by /health so you always know which version is running
APP_VERSION=1.0.0
